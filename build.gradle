allprojects {
    apply plugin: 'java'
    version = '1.0.1-SNAPSHOT'
}

dependencies {
        compile project(':api')
}

project(':api') {
     tasks.withType(Jar) {
            destinationDir = rootProject.jar.destinationDir
    }
}
